# Deployment

Current deployment target:

- local development via `swift run A2DClock`

Planned release target:

- signed and notarized macOS screensaver bundle
- GitHub release artifact for direct install

Do not treat the current host app as the final distribution format.
