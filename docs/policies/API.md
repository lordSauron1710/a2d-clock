# API

The current repository exposes no network API.

If future work adds:

- update checks
- licensing
- telemetry
- sync

document the external endpoints, payloads, and failure rules here before shipping.
